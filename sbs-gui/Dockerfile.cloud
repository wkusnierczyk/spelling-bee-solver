FROM nginx:alpine

# Copy Nginx Template
COPY nginx.conf.template /etc/nginx/templates/default.conf.template

# Copy ALL artifacts (Recursive)
COPY composeApp/build/dist/js/productionExecutable/ . /usr/share/nginx/html/

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
